% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{mcma_formula}
\alias{mcma_formula}
\title{Build the brms nonlinear formula}
\usage{
mcma_formula(
  bounded = FALSE,
  prev_re = ~(1 | es_id),
  sesp_re = ~(1 | es_id),
  correlated_re = TRUE,
  moderators = NULL,
  measure_col = "measure_id",
  gold_column = "is_gold",
  comparison = FALSE
)
}
\arguments{
\item{bounded}{Logical; if TRUE, uses Se = 0.5 + 0.5 * inv_logit(eta).}

\item{prev_re}{One-sided formula for prevalence random effects, e.g.,
\code{~ (1 | es_id)}. Set to NULL to omit.}

\item{sesp_re}{One-sided formula for Se/Sp random effects. Set to NULL
to omit (M5-equivalent).}

\item{correlated_re}{Logical; if TRUE, uses \code{(1 | s | ...)} to correlate
pi, Se, and Sp random effects.}

\item{moderators}{One-sided formula for moderator fixed effects, e.g.,
\code{~ setting + age_centered}. Set to NULL for no moderators.}

\item{measure_col}{Column name in data identifying the measure.}

\item{gold_column}{Column name flagging gold-standard studies; used only when
\code{comparison = TRUE}. Defaults to \code{"is_gold"}.}

\item{comparison}{Logical; if TRUE, builds comparison formula with
\code{pi ~ 0 + is_gold} for separate gold/screen intercepts.}
}
\value{
A brms \code{bf()} formula object.
}
\description{
Returns the \code{brms::bf()} object that \code{mcma_fit()} would construct, without
fitting the model. Useful for inspection, debugging, or manual modification.
}

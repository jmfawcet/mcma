% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_bias}
\alias{extract_bias}
\title{Extract the concealment and over-diagnosis terms from a joint fit}
\usage{
extract_bias(fit, summary = TRUE, probs = c(0.025, 0.975))
}
\arguments{
\item{fit}{A brmsfit object returned by \code{\link[=mcma_fit_joint]{mcma_fit_joint()}}.}

\item{summary}{Logical; if FALSE, returns a list with the raw posterior
draws of each term (\code{NULL} for a term without a prior).}

\item{probs}{Quantile probabilities for the CI bounds.}
}
\value{
A tibble with one row per term (\code{c}, \code{o}) giving \code{mode}
(\"none\", \"fixed\" or \"prior\"), the prior string, and summary
statistics, or a list of draws when \code{summary = FALSE}.
}
\description{
Summarises the concealment probability \code{c} and the over-diagnosis
(additional false-positive) probability \code{o} used by \code{\link[=mcma_fit_joint]{mcma_fit_joint()}}.
A term given a prior is summarised from its posterior draws; a fixed term
is reported at its fixed value (the mean of the row-specific values, with
their range as bounds, when it varies by row); an absent term is reported
as zero.
}

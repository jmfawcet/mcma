% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias.R
\name{rg_correct_bias}
\alias{rg_correct_bias}
\title{Rogan-Gladen correction with concealment and additional false positives}
\usage{
rg_correct_bias(
  p_obs,
  se,
  sp,
  c = 0,
  o = 0,
  gamma = 1,
  delta = 1,
  clamp = FALSE
)
}
\arguments{
\item{p_obs}{Observed prevalence.}

\item{se, sp}{Baseline sensitivity and specificity, before the additional
concealment and false-positive processes.}

\item{c}{Concealment probability in diagnostic interviews.}

\item{o}{Additional false-positive probability in diagnostic interviews.}

\item{gamma, delta}{Screening weights for concealment and additional false
positives, respectively. Each is between zero and one. In particular,
\code{o * delta} is the probability that the additional process makes an
otherwise correctly classified non-case screen positive.}

\item{clamp}{Logical; if TRUE, truncate estimates to \verb{[0, 1]}. Defaults to
FALSE because truncation can introduce bias.}
}
\value{
Corrected prevalence estimate(s).
}
\description{
Applies the correction using effective sensitivity \code{(1 - c * gamma) * se}
and effective specificity \code{(1 - o * delta) * sp}.
}
\details{
All probability inputs must be finite and in \verb{[0, 1]}, and have
length one or a common vector length. The effective sensitivity plus
effective specificity must differ from one. Negative denominators are
allowed. For diagnostic interviews, use \code{se = sp = 1} and
\code{gamma = delta = 1}, giving \code{(p_obs - o) / (1 - c - o)}.
With \code{c = o = 0}, this is the ordinary Rogan-Gladen correction.
Inputs are treated as known; this function does not propagate their
uncertainty. Do not apply these additional effects to accuracy estimates
that already include the same effects.
}
\examples{
rg_correct_bias(0.137008, se = 0.85, sp = 0.89,
                c = 0.30, o = 0.02, gamma = 0.5, delta = 0.5)
}
\seealso{
\code{\link[=rg_correct]{rg_correct()}}, \code{\link[=mcma_fit_joint]{mcma_fit_joint()}}
}

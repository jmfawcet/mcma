% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{clamp_probability}
\alias{clamp_probability}
\title{Clamp values to optional limits}
\usage{
clamp_probability(p, lower = NULL, upper = NULL)
}
\arguments{
\item{p}{Numeric vector of values to clamp.}

\item{lower}{Numeric lower limit, or \code{NULL} to omit the lower bound.}

\item{upper}{Numeric upper limit, or \code{NULL} to omit the upper bound.}
}
\value{
A numeric vector with the requested limits applied. With scalar
limits, the result has the same length as \code{p}.
}
\description{
Replaces values below a lower limit or above an upper limit with that limit.
This internal helper keeps accuracy prior centres within their allowed ranges.
}
\details{
The lower bound is applied first with \code{\link[base:pmax]{base::pmax()}}, followed by
the upper bound with \code{\link[base:pmin]{base::pmin()}}. Scalar limits apply to every element;
vector limits follow the usual R recycling rules. Callers must supply
compatible limits with \code{lower <= upper} when both bounds are present.
Inputs are not validated and missing values propagate.

With both limits set to \code{NULL}, \code{p} is returned unchanged. The function
only enforces the supplied limits; it does not automatically restrict
values to the probability range \code{0} to \code{1}.
}
\examples{
mcma:::clamp_probability(c(-0.1, 0.25, 1.2), lower = 0, upper = 1)
mcma:::clamp_probability(c(0, 0.2, 1), lower = 0.01)
}
\seealso{
\code{\link[=safe_qlogis]{safe_qlogis()}}, \code{\link[=map_to_inner]{map_to_inner()}}
}
\keyword{internal}

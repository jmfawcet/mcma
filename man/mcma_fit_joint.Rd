% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{mcma_fit_joint}
\alias{mcma_fit_joint}
\title{Fit a joint model (M8) estimating Se/Sp from data}
\usage{
mcma_fit_joint(
  data,
  gold_column = "is_gold",
  prev_center,
  se_prior_center = 0.8,
  sp_prior_center = 0.8,
  se_prior_sd = 1,
  sp_prior_sd = 1,
  prev_re = ~(1 | es_id),
  sesp_re = ~(1 | es_id),
  correlated_re = TRUE,
  bounded = TRUE,
  study_col = "es_id",
  file = NULL,
  iter = 6000,
  warmup = floor(iter/2),
  adapt_delta = 0.9999,
  max_treedepth = 20,
  chains = 4,
  cores = 4,
  backend = "cmdstanr",
  refresh = 50,
  c = 0,
  o = 0,
  gamma = 1,
  delta = 1,
  c_prior = NULL,
  o_prior = NULL,
  step_size = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame with y, n, study/es_id, and a gold indicator column.}

\item{gold_column}{Name of the logical column identifying gold-standard
studies.}

\item{prev_center}{Prevalence prior centre (probability scale).}

\item{se_prior_center}{Prior centre for screening Se.}

\item{sp_prior_center}{Prior centre for screening Sp.}

\item{se_prior_sd}{Prior SD for screening Se on the (inner) logit scale.}

\item{sp_prior_sd}{Prior SD for screening Sp on the (inner) logit scale.}

\item{prev_re}{One-sided formula for prevalence random effects.}

\item{sesp_re}{One-sided formula for Se/Sp random effects.}

\item{correlated_re}{Logical; if TRUE, REs are correlated across submodels.}

\item{bounded}{Logical; whether to use the bounded parameterization.}

\item{study_col}{Column used by the default prevalence and accuracy
random-effects formulas. Explicitly supplied formulas are used unchanged.}

\item{file}{File path for caching. Cached RDS files retain the package
configuration needed by the extraction functions.}

\item{iter}{Total iterations per chain.}

\item{warmup}{Warmup iterations per chain.}

\item{adapt_delta}{Target acceptance rate.}

\item{max_treedepth}{Maximum tree depth.}

\item{chains}{Number of MCMC chains.}

\item{cores}{Number of cores.}

\item{backend}{brms backend.}

\item{refresh}{Iteration interval for progress printing (0 = silent).}

\item{c, o}{Fixed interview concealment and additional false-positive
probabilities, respectively. Each is a scalar or a vector of length
\code{nrow(data)}, with values in [0, 1]. Zero (the default) removes the term.}

\item{gamma, delta}{Fixed screening weights in [0, 1], scalar or length
\code{nrow(data)}. Gold rows always use weight one. \code{o * delta} is the
probability of making an otherwise correctly classified non-case screen
positive. See \code{\link[=rg_correct_bias]{rg_correct_bias()}}.}

\item{c_prior, o_prior}{Optional prior for the concealment and
over-diagnosis parameters, as a Stan distribution string on the
probability scale (e.g. \code{"beta(4, 12)"}); the parameter is bounded to
[0, 1]. \code{NULL} (the default) keeps the term fixed at \code{c} / \code{o}. A prior
and a non-zero fixed value cannot both be given for the same term.}

\item{step_size}{Optional initial sampler step size; NULL lets Stan choose it.}

\item{...}{Additional arguments passed to \code{brms::brm()}.}
}
\value{
A brmsfit object.
}
\description{
Fits the joint model that simultaneously estimates prevalence, Se, and Sp.
Uses an accuracy-group parameterization (gold vs screen) rather than
per-measure Se/Sp coefficients.
}
\details{
Concealment (\code{c}) and over-diagnosis / additional false positives
(\code{o}) enter the observation model as multipliers on sensitivity and
specificity: gold rows use \code{1 - c} and \code{1 - o}, screening rows use
\code{1 - c * gamma} and \code{1 - o * delta}. Each term can be handled in one of
three ways: absent (\code{c = 0} / \code{o = 0} with no prior, the default, which
reproduces the original model), fixed at a known value (\code{c} / \code{o}, scalar
or per row), or treated as a single model parameter in [0, 1] with a prior
(\code{c_prior} / \code{o_prior}, e.g. \code{"beta(4, 12)"}). These parameters are
estimated jointly with prevalence and accuracy. The likelihood can update
their distributions even when they are not separately identifiable, so
their posteriors need not reproduce their priors. This route propagates
their joint posterior uncertainty into the prevalence estimate within a
single fit. \code{gamma} and \code{delta} are always fixed. Baseline gold accuracy
retains the existing near-perfect specification. Screening priors and
extracted Se/Sp refer to baseline accuracy before these additional
effects; use \code{\link[=extract_bias]{extract_bias()}} to summarise the terms themselves. Bounding
applies to baseline accuracy, not the effective accuracy after adjustment.
When reusing a cache path across scenarios, pass
\code{file_refit = "on_change"} through \code{...}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{as_brms_prior}
\alias{as_brms_prior}
\title{Convert mcma_priors to brms prior}
\usage{
as_brms_prior(
  priors,
  prev_center,
  bounded = FALSE,
  prev_prior_sd = 1.5,
  tau_prior_sd = 1,
  sesp_re_sd = 0.5,
  moderator_terms = NULL,
  moderator_prior_sd = 1,
  has_prev_re = TRUE,
  has_sesp_re = TRUE,
  sigma_bias = 0,
  delta_se = 0,
  delta_sp = 0,
  rho_bias = -0.5,
  sd_method = "chain_rule",
  measure_col = "measure_id"
)
}
\arguments{
\item{priors}{An mcma_priors object.}

\item{prev_center}{Prevalence prior centre on the probability scale.}

\item{bounded}{Logical; if TRUE, uses bounded parameterization.}

\item{prev_prior_sd}{SD of the prevalence prior on the logit scale.}

\item{tau_prior_sd}{SD of the prior on the prevalence RE SD.}

\item{sesp_re_sd}{SD of the prior on Se/Sp RE SDs.}

\item{moderator_terms}{Character vector of moderator variable names.}

\item{moderator_prior_sd}{SD of the prior on moderator coefficients.}

\item{has_prev_re}{Logical; whether the model includes prevalence REs.}

\item{has_sesp_re}{Logical; whether the model includes Se/Sp REs.}

\item{sigma_bias}{Random perturbation SD for prior centres on logit scale.}

\item{delta_se}{Systematic shift to Se prior centres on probability scale.}

\item{delta_sp}{Systematic shift to Sp prior centres on probability scale.}

\item{rho_bias}{Correlation of Se/Sp bias perturbations.}

\item{sd_method}{Method used when bounded = TRUE. "simple" applies the
approximation directly to t = 2p - 1; "chain_rule" propagates uncertainty
from p through the bounded transformation. "simple" should not be used
and is included for legacy purposes.}

\item{measure_col}{Name of the instrument identifier column used in the
model formula. It determines the coefficient names to which the
instrument-specific accuracy priors are attached.}
}
\value{
A brms prior object.
}
\description{
Translates an \code{mcma_priors} object into a \code{brms} prior object. This is
called internally by \code{mcma_fit()} but is exported for debugging.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{mcma_sim_summarise}
\alias{mcma_sim_summarise}
\title{Summarise simulation results}
\usage{
mcma_sim_summarise(
  results,
  group_vars = NULL,
  summary_type = c("prevalence", "moderator"),
  exclude_bad = FALSE,
  bad_criteria = NULL,
  rhat_limit = 1.01,
  min_ess = 400,
  max_divergences = 0
)
}
\arguments{
\item{results}{List of simulation results from \code{mcma_sim_read()}.}

\item{group_vars}{Variables to group by; NULL uses \code{"scenario_id"}.}

\item{summary_type}{\code{"prevalence"} or \code{"moderator"}.}

\item{exclude_bad}{Logical; exclude fits flagged by the convergence criterion.}

\item{bad_criteria}{Optional function taking a saved replicate record and
returning TRUE when it should be flagged. NULL uses the default checks.}

\item{rhat_limit}{Largest acceptable Rhat (default 1.01).}

\item{min_ess}{Smallest acceptable bulk or tail ESS (default 400);
zero disables this check.}

\item{max_divergences}{Largest acceptable divergence count (default zero).}
}
\value{
A list containing grouped \verb{$summary}, all extractable \verb{$replicates}
(with an \code{included} flag), \verb{$bad_reps} with reasons, and \verb{$n_bad} counting
replicate records rather than coefficients. Summary interval endpoints are
averages of replicate endpoints, not an interval for the simulation mean.
\code{rejection_rate} is the fraction of moderator intervals excluding zero;
it estimates power for a nonzero slope and type-I error for a zero slope.
}
\description{
Groups replicate estimates by design factors, with bias, RMSE and coverage
when generation truth is available. Failed fits and extraction failures are
always recorded; extractable fits with poor convergence are retained only
when \code{exclude_bad = FALSE}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{mcma_fit}
\alias{mcma_fit}
\title{Fit a misclassification-corrected meta-analysis model}
\usage{
mcma_fit(
  data,
  priors = NULL,
  prev_center,
  prev_re = ~(1 | es_id),
  moderators = NULL,
  sesp_re = ~(1 | es_id),
  correlated_re = TRUE,
  bounded = TRUE,
  sd_method = "chain_rule",
  measure_col = "measure_id",
  iter = 6000,
  warmup = floor(iter/2),
  adapt_delta = 0.9999,
  max_treedepth = 20,
  chains = 4,
  cores = 4,
  backend = "cmdstanr",
  file = NULL,
  refresh = 50,
  step_size = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame with columns: y, n, study/effect-size IDs, measure
IDs, and any moderators.}

\item{priors}{An mcma_priors object, or NULL for a naive model. Prior rows
for instruments absent from data are ignored with a warning.}

\item{prev_center}{Prevalence prior centre on the probability scale.}

\item{prev_re}{One-sided formula for prevalence random effects.}

\item{moderators}{One-sided formula for moderator fixed effects.}

\item{sesp_re}{One-sided formula for Se/Sp random effects.}

\item{correlated_re}{Logical; if TRUE, correlates RE across submodels.}

\item{bounded}{Logical; if TRUE (default), uses the bounded parameterization
(Se, Sp in [0.5, 1)) used throughout the paper.}

\item{sd_method}{Method for converting accuracy-prior uncertainty to the
fitted scale when \code{bounded = TRUE}. \code{"chain_rule"} propagates the
probability-scale variance through the full bounded transformation.
\code{"simple"} is retained for legacy comparisons; see
\code{\link[=prior_sd_from_kappa]{prior_sd_from_kappa()}}. Ignored for a naive fit.}

\item{measure_col}{Column name for the measure identifier.}

\item{iter}{Total iterations per chain.}

\item{warmup}{Warmup iterations per chain.}

\item{adapt_delta}{Target acceptance rate.}

\item{max_treedepth}{Maximum tree depth.}

\item{chains}{Number of MCMC chains.}

\item{cores}{Number of cores.}

\item{backend}{brms backend.}

\item{file}{File path for model caching. Cached RDS files retain the package
configuration and priors needed by the extraction functions.}

\item{refresh}{Iteration interval for progress printing (0 = silent).}

\item{step_size}{Optional initial sampler step size; NULL lets Stan choose it.}

\item{...}{Additional arguments passed to \code{brms::brm()}.}
}
\value{
A brmsfit object with the mcma_priors attached as an attribute.
}
\description{
The primary fitting function. When \code{priors} is NULL, fits a naive
(uncorrected) binomial model. When priors are supplied, constructs the
nonlinear formula and fits the corrected model.
}

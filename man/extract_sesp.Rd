% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_sesp}
\alias{extract_sesp}
\title{Extract Se/Sp posterior summaries}
\usage{
extract_sesp(
  fit,
  bounded = NULL,
  level = c("per_measure", "global"),
  summary = TRUE,
  probs = c(0.025, 0.975),
  measure_col = "measure_id",
  newdata = NULL,
  re_formula = NULL,
  re.form = NULL,
  allow_new_levels = FALSE,
  sample_new_levels = "gaussian"
)
}
\arguments{
\item{fit}{A brmsfit object (corrected or joint model).}

\item{bounded}{Logical; if NULL, auto-detected from the fit.}

\item{level}{\code{"global"} returns a single estimate (joint models);
\code{"per_measure"} returns one row per screening measure. When omitted,
joint models use \code{"global"} and other models use \code{"per_measure"}.}

\item{summary}{Logical; if TRUE, returns summary tibble.}

\item{probs}{Quantile probabilities.}

\item{measure_col}{Name of the instrument identifier column used when
fitting a per-measure model. This selects the matching Se/Sp coefficients;
it is not used for a joint model's global accuracy summary.}

\item{newdata}{Optional data frame for prediction. Explicitly supplying
\code{newdata} (including \code{NULL}), \code{re_formula}, or \code{re.form} selects brms-style
prediction. \code{NULL} uses the original model data. Include all columns
required by the fitted brms model, including \code{n} and \code{measure_id} or
\code{acc_group} where applicable; copying rows of \code{fit$data} is a convenient
starting point. Set moderators to the values of interest. Existing group
labels use fitted effects; unseen labels require \code{allow_new_levels = TRUE}.}

\item{re_formula}{Formula selecting group-level terms, as in brms. \code{NULL}
includes all terms, \code{NA} or \code{~0} excludes all, and \code{~(1 | es_id)} includes
study effects while holding omitted report effects at zero. Excluding an
effect does not average over its heterogeneity.}

\item{re.form}{Alias of \code{re_formula}. Supply only one of these arguments.}

\item{allow_new_levels}{Logical; allow unseen group labels in \code{newdata}.
Defaults to \code{FALSE}, as in brms.}

\item{sample_new_levels}{How brms generates new group effects. Defaults to
\code{"gaussian"}: draw from the fitted normal distribution of group effects.
Alternatives are \code{"uncertainty"} (select an existing group's effect at
each posterior draw) and \code{"old_levels"} (use one existing group's full
set of draws per new level). These alternatives require a brms-style
prediction request.}
}
\value{
A tibble or list of draw vectors. With explicit \code{newdata},
\code{re_formula}, or \code{re.form}, returns one summary row per data row, identified
by \code{.row}, or a list containing \code{se} and \code{sp} draws-by-row matrices.
}
\description{
Extracts sensitivity and specificity posteriors from corrected or joint
models.
}
\details{
Without prediction inputs, \code{level} selects the original coefficient
summaries. With prediction inputs, rows determine the accuracy target and
\code{level} is not used. These predictions describe baseline Se/Sp, before any
additional c/o adjustment. For joint models, \code{acc_group} identifies gold
or screening rows. Both accuracy parameters use the same sampled group
effects so their posterior dependence is retained.
}

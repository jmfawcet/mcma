% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{safe_qlogis}
\alias{safe_qlogis}
\title{Logit transformation with finite boundary values}
\usage{
safe_qlogis(p, eps = 1e-04)
}
\arguments{
\item{p}{Numeric vector of probabilities. Values outside the probability
range are clipped to the specified limits as well.}

\item{eps}{Positive numeric scalar smaller than \code{0.5}. Probabilities are
clipped to \code{eps} and \code{1 - eps} before transformation; defaults to \code{1e-4}.}
}
\value{
A numeric vector of log odds with the same length as \code{p}.
}
\description{
Clamps probabilities away from zero and one before converting them to log
odds with \code{\link[stats:qlogis]{stats::qlogis()}}.
}
\details{
This internal helper calls \code{\link[=clamp_probability]{clamp_probability()}} and then applies
\code{\link[stats:qlogis]{stats::qlogis()}}. With the default \code{eps}, inputs of zero and one have
finite log odds of approximately -9.21 and 9.21. Missing inputs remain
missing. The boundary values depend on \code{eps}. Callers
are responsible for supplying a valid \code{eps}; inputs are not validated.
}
\examples{
mcma:::safe_qlogis(c(0, 0.5, 1))
mcma:::safe_qlogis(c(0.01, 0.25, 0.99))
}
\seealso{
\code{\link[=clamp_probability]{clamp_probability()}}, \code{\link[stats:qlogis]{stats::qlogis()}}
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract_prevalence}
\alias{extract_prevalence}
\title{Extract or predict prevalence}
\usage{
extract_prevalence(
  fit,
  summary = TRUE,
  prediction_interval = FALSE,
  probs = c(0.025, 0.975),
  transform = TRUE,
  prediction_groups = NULL,
  condition_on = NULL,
  gold_column = NULL,
  zero_groups = NULL,
  newdata = NULL,
  re_formula = NULL,
  re.form = NULL,
  allow_new_levels = FALSE,
  sample_new_levels = "gaussian"
)
}
\arguments{
\item{fit}{A brmsfit object.}

\item{summary}{Logical; if TRUE, returns a summary tibble. If FALSE,
returns raw posterior draws on the scale selected by \code{transform}.}

\item{prediction_interval}{Logical; if TRUE, adds \code{pi_l95} and \code{pi_u95}.
For a brms-style prediction request, these are the same target-draw
quantiles as \code{l95} and \code{u95}. Otherwise, the original new-study PI is used.}

\item{probs}{Quantile probabilities for CI/PI bounds.}

\item{transform}{Logical; if TRUE, returns probability scale.}

\item{prediction_groups}{Legacy argument: names of grouping factors whose levels are new.
NULL includes every prevalence grouping factor not in \code{condition_on}
or \code{zero_groups}.}

\item{condition_on}{Legacy argument: named list of existing group levels, for example
\code{list(refid = "Smith2020")}. Their fitted prevalence deviations are
retained in predictions, including posterior uncertainty. Every grouping
factor must be new, conditioned on, or explicitly held at zero when
requesting a PI.}

\item{gold_column}{Gold indicator column; NULL uses the saved configuration
or \code{"is_gold"} for older fits.}

\item{zero_groups}{Legacy argument: names of grouping factors whose random effects are fixed
at zero, for example \code{"refid"}. These contribute neither a fitted deviation
nor heterogeneity to the prediction interval. NULL holds no groups at zero.}

\item{newdata}{Optional data frame for prediction. Explicitly supplying
\code{newdata} (including \code{NULL}), \code{re_formula}, or \code{re.form} selects brms-style
prediction. \code{NULL} uses the original model data. Include all columns
required by the fitted brms model, including \code{n} and \code{measure_id} or
\code{acc_group} where applicable; copying rows of \code{fit$data} is a convenient
starting point. Set moderators to the values of interest. Existing group
labels use fitted effects; unseen labels require \code{allow_new_levels = TRUE}.}

\item{re_formula}{Formula selecting group-level terms, as in brms. \code{NULL}
includes all terms, \code{NA} or \code{~0} excludes all, and \code{~(1 | es_id)} includes
study effects while holding omitted report effects at zero. Excluding an
effect does not average over its heterogeneity.}

\item{re.form}{Alias of \code{re_formula}. Supply only one of these arguments.}

\item{allow_new_levels}{Logical; allow unseen group labels in \code{newdata}.
Defaults to \code{FALSE}, as in brms.}

\item{sample_new_levels}{How brms generates new group effects. Defaults to
\code{"gaussian"}: draw from the fitted normal distribution of group effects.
Alternatives are \code{"uncertainty"} (select an existing group's effect at
each posterior draw) and \code{"old_levels"} (use one existing group's full
set of draws per new level). These alternatives require a brms-style
prediction request.}
}
\value{
A summary tibble or numeric vector of draws. Comparison models
return one summary row per \code{population}, or a list with \code{gold} and \code{screen}
draw vectors. Population summaries remain at reference moderator values;
\code{condition_on} and \code{zero_groups} affect only the legacy prediction interval.
A brms-style request instead returns one summary row per data row, identified
by \code{.row}, or a draws-by-row matrix when \code{summary = FALSE}. All summaries
and draws describe the requested moderators and random effects.
}
\description{
Extracts pooled prevalence or predicts latent prevalence for specified data
rows and random effects. Handles naive models
(\code{b_Intercept}), corrected models (\code{b_pi_Intercept}), and comparison
models (separate gold/screen intercepts).
}
\details{
With all groups new, random-intercept variances are added within
each posterior draw. For a new estimate within an existing report, use
\code{prediction_groups = "es_id"} and \code{condition_on = list(refid = "reportID")}.
To include study heterogeneity while holding the report effect at zero,
use \code{prediction_groups = "es_id"} and \code{zero_groups = "refid"}. This fixes
the report deviation at its population centre on the log-odds scale; it
does not average over report heterogeneity. Intercept and selected SD
uncertainty remain in the interval. Prediction intervals describe latent prevalence,
without binomial noise.
}
\examples{
\dontrun{
nd <- fit$data[1, , drop = FALSE]
nd$es_id <- "new_study"
extract_prevalence(fit, newdata = nd, re_formula = ~(1 | es_id),
                   allow_new_levels = TRUE, sample_new_levels = "gaussian",
                   prediction_interval = TRUE)
}
}
